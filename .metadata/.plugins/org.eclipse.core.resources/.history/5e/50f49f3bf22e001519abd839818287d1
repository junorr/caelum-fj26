<!DOCTYPE html>
<html xmlns:h="http://java.sun.com/jsf/html"
	xmlns:f="http://java.sun.com/jsf/core">
	
<h:head>
	<meta charset="UTF-8"/>
	<title>SisNF</title>
</h:head>

<h:body>
	<h:form>
	<h:panelGrid columns="2">
		<h:outputLabel value="Nome:" for="nome"/>
		<h:inputText id="nome" value="#{produtoBean.produto.nome}"/>

		<h:outputLabel value="Descrição:" for="descricao"/>		
		<h:inputTextarea id="descricao" value="#{produtoBean.produto.descricao}"/>
		
		<h:outputLabel value="Preço:" for="preco"/>
		<h:inputText id="preco" value="#{produtoBean.produto.preco}"/>
		
		<h:commandButton value="Gravar" action="#{produtoBean.grava}"/>
		<h:outputText id="outmsg" value="[#{produtoBean.message}]"/>
	</h:panelGrid>
	</h:form>
	
	<h:dataTable value="produtoBean.produtos" var="pd">
		<h:column>#{pd.nome}</h:column>
		<h:column>#{pd.descricao}</h:column>
		<h:column>#{pd.preco}</h:column>
	</h:dataTable>
</h:body>
</html>